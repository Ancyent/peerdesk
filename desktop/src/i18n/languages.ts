// Native language names (autonyms), intentionally NOT translated. Single home;
// the anti-Romanian source gate excludes this file.
import { SUPPORTED_LANGUAGES, type Language } from './index';
export const LANGUAGE_NAMES: Record<Language, string> = { en: 'English', ro: 'Română' };
export { SUPPORTED_LANGUAGES };
export type { Language };
