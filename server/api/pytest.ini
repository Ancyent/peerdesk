[pytest]
asyncio_mode = auto
markers =
    migration: runs the real Alembic migrations against a throwaway postgres:16-alpine Docker container (slow; skips cleanly if Docker is unavailable). Deselect with -m "not migration".
