target/
__pycache__/
*.pyc
node_modules/
dist/
.env
.venv/
*.env.local
builds/
peerdesk-release.jks
android-secrets.env
screenshots/

# Manual test checklists and infra notes — local only, never published.
# Anything meant for users goes in docs/.
TESTS/

# Local tooling / editor / OS junk — not part of the project
.claude/
.superpowers/
graphify-out/
.DS_Store
.idea/
.vscode/
*.log
*.pid
docs/superpowers/
no-push/

# Backups of .env made before edits — same secrets as the original, so they
# must never be stageable.
.env.bak-*
*.env.bak-*
